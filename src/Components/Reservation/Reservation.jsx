import classes from './Reservation.module.css';

const Reservation = () => {
	return (
    <div className={classes.container}>
        <p>Reserve here!</p>
    </div>
    );
};

export default Reservation;
